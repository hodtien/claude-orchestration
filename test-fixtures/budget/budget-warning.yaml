# Budget config where actual usage triggers WARNING
global:
  daily_token_limit: 3000
  alert_threshold_pct: 80
  hard_cap_pct: 100

per_model:
  oc-high:
    daily_limit: 2000

reporting:
  rollup_window: 24h
