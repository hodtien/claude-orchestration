# Budget config where actual usage triggers OVER_BUDGET
global:
  daily_token_limit: 2000
  alert_threshold_pct: 80
  hard_cap_pct: 100

per_model:
  oc-high:
    daily_limit: 1000

reporting:
  rollup_window: 24h
