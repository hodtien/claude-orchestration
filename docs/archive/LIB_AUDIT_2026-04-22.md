# lib/ Audit — 2026-04-22

## Summary

- Total libs: 16 | **2,670 lines total**
- **Wired** (sourced in active scripts): 4
- **Drafted** (code exists, not wired): 3
- **Deprecated** (only in bin/deprecated/ or referenced-only): 9
- Total wired: ~588 lines | Drafted: ~532 lines | Deprecated: ~1,550 lines

Recommendation: keep all 16 for now. 4 are wired, 3 are candidates for wiring
in the next sprint. 8 are safely deprecated. 1 needs review.

---

## Detailed Table

| Lib | Lines | Called By | Status | Recommendation |
|---|---|---|---|---|
| triage-tiers.sh | 85 | task-dispatch.sh (active) | **Wired** | Keep. Active part of dispatch flow. |
| intent-verifier.sh | 184 | task-dispatch.sh (active) | **Wired** | Keep. Intent check before dispatch. |
| cost-tracker.sh | 215 | task-dispatch.sh (active), orch-cost-dashboard.sh, _dashboard/cost.sh | **Wired** | Keep. Active cost tracking. |
| agent-failover.sh | 104 | task-dispatch.sh (active) | **Wired** | Keep. Failover on agent error. |
| dag-healer.sh | 173 | NONE | Drafted | Wire into task-dispatch when DAG execution is stable. Low priority. |
| context-compressor.sh | 192 | NONE | Drafted | Wire when context budget becomes a problem. Keep in lib/. |
| consensus-vote.sh | 76 | NONE (bin/deprecated/ callers only) | Drafted | Wire when `parallel_policy.pick_strategy: consensus` is enabled. |
| state-conflict-resolver.sh | 91 | DEPRECATED → lib/deprecated/ | Drafted | Moved to lib/deprecated/ 2026-04-23. No active callers confirmed.
| task-decomposer.sh | 344 | NONE | Deprecated | Keep in lib/. Move to lib/deprecated/ after task-decomposer batch review. |
| learning-engine.sh | 259 | NONE | Deprecated | Keep in lib/. Insufficient data to learn from at solo-dev scale. |
| cross-project.sh | 202 | NONE | Deprecated | Keep in lib/. Solo-dev use case doesn't need this yet. |
| sprint-queue.sh | 233 | NONE | Deprecated | Keep in lib/. Parallel sprints add complexity beyond solo-dev need. |
| speculation-buffer.sh | 108 | NONE | Deprecated | Only in breakthrough-ideas docs. No active callers. |
| discarded-alternatives.sh | 111 | NONE | Deprecated | Only in bin/deprecated/consensus-trigger.sh (commented shellcheck). |
| style-memory.sh | 143 | NONE | Deprecated | Only in bin/deprecated/style-memory-*.sh (commented shellcheck). |
| provenance-tracker.sh | 150 | NONE | Deprecated | Only in bin/deprecated/provenance-*.sh (commented shellcheck). |

---

## Wiring Candidates (next sprint)

### High Priority

1. **agent-failover.sh** — Already wired in task-dispatch.sh at line 30.
   Verify the `failover_find_available` function is actually called on error.

2. **cost-tracker.sh** — Already wired in task-dispatch.sh at line 29.
   Verify `cost_record` is called after each successful dispatch.

3. **intent-verifier.sh** — Already wired in task-dispatch.sh at line 27.
   Verify `verify_spec` / `verify_task_intent` is called before dispatch loop.

### Medium Priority

4. **state-conflict-resolver.sh** — ~~Review the file, if it resolves task-dispatch~~ → **DEPRECATED 2026-04-23**: Moved to `lib/deprecated/state-conflict-resolver.sh`. No active callers confirmed. No further action needed.
   conflicts, wire after testing. If it was a speculative experiment, move to
   lib/deprecated/.

5. **context-compressor.sh** — Wire into task-dispatch when:
   - Token budget monitoring is enabled
   - There's evidence of context overflow in recent batches

### Low Priority

6. **consensus-vote.sh** — Wire only when `parallel_policy.pick_strategy: consensus`
   is switched from `first_success`. Currently `first_success` is the default.

7. **dag-healer.sh** — Wire when task-dispatch DAG execution is mature enough
   to benefit from self-healing.

---

## Deprecated libs — keep or delete?

These 8 libs have no active callers. Options:

- **Keep in lib/**: Safe option. Takes negligible space. Easy to re-enable if needed.
- **Move to lib/deprecated/**: Cleaner lib/ directory. Same code, just organized.
- **Delete**: Only if you're certain they'll never be used.

**Recommendation:** Move all 8 to `lib/deprecated/` in a follow-up cleanup task.
This makes the active lib/ surface area smaller and clearer.

---

## Audit Method

```bash
# Find active callers (wired)
grep -n "source.*lib/" bin/task-dispatch.sh bin/_dashboard/*.sh

# Find deprecated callers
grep -rn "shellcheck source=.*lib/" bin/deprecated/

# Count lines per lib
wc -l lib/*.sh
```

---

## Action Items

- [ ] **Review** `lib/state-conflict-resolver.sh` — ~~wire or shelve~~ → **DEPRECATED 2026-04-23**: Moved to `lib/deprecated/`. No active callers.
- [ ] **Verify** agent-failover, cost-tracker, intent-verifier actually fire in dispatch
- [ ] **Move** 8 deprecated libs to `lib/deprecated/` (follow-up task)
- [ ] **Update** CLAUDE.md if wiring decisions change active lib/ surface
