task_type: code_review
description: Should detect unused variable
input: "Review this Python code:\n```python\ndef calculate(x, y):\n    z = x + y\n\
  \    return z\n\nresult = calculate(10, 20)\nprint(result)\n```\n"
expected_properties:
  must_contain:
  - z
  must_not_contain:
  - TODO
  - FIXME
  regex_match: (unused|unnecessary|not used)
timeout: 60
