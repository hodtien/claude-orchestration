---
description: Alias for /dispatch. Auto-plan and dispatch a DAG of tasks via the hybrid resolver.
---

Run `/dispatch $ARGUMENTS`.

See `commands/dispatch.md` for the full protocol — this command is a thin alias preserved so `/orchestration <work>` produces identical behavior to `/dispatch <work>`.
